export interface EmailTemplateProps {
	name: string;
	email: string;
	phone: string;
	product: string;
	quantity: string;
	deadline: string;
	location: string;
	hasDesign: string;
	message: string;
}

// {
// 	name,
// 		email,
// 		phone,
// 		product,
// 		quantity,
// 		deadline,
// 		location,
// 		hasDesign,
// 		message
// }: EmailTemplateProps

const EmailTemplate = () => {
	return (
		<div>
		
		</div>
	)
}
export default EmailTemplate
